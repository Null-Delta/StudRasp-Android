<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zednull.timetable" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true">

        <activity
            android:name="com.zednull.timetable.LaunchActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SplashTheme">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.zednull.timetable.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.TimeTable" >

        </activity>
        <activity
            android:name="com.zednull.timetable.LoadTimeTableActivity"
            android:exported="true"
            android:theme="@style/Theme.TimeTable" >
        </activity>
    </application>

</manifest>